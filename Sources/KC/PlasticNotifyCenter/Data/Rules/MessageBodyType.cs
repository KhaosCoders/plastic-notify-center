namespace PlasticNotifyCenter.Data
{
    /// <summary>
    /// List of possible message body types
    /// </summary>
    public enum MessageBodyType
    {
        // Plain text
        Plain = 0,

        // HTML message
        HTML = 1
    }
}